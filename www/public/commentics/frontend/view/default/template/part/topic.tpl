<div class="cmtx_topic_block">
    @if rich_snippets_enabled
        <span class="cmtx_topic_intro">{{ lang_text_topic_intro }}</span> <span class="cmtx_topic" itemprop="name">{{ topic }}</span>
    @else
        <span class="cmtx_topic_intro">{{ lang_text_topic_intro }}</span> <span class="cmtx_topic">{{ topic }}</span>
    @endif
</div>